import React from 'react';
import { Col, Row, Button, Image, Container, Input } from 'react-bootstrap';
import Content from './components/Content';

const App = () => {
  return (
    <Container>
      <Content />
    </Container>
  );
};

export default App;
